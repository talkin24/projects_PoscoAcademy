## git 기본 사용법

#### A3 김채은

- git 이란?

  - 깃허브 구성 소프트웨어
  - 프로젝트의 어떤 부분도 겹쳐쓰지 않게 프로젝트의 변경을 관리하는 버전관리 소프트웨어

- 주요 명령어

  - git init : 깃 저장소 초기화

  - git help : 입력 시 21개의 주요 명령어들 나타남. git help (명령어)와 같이 입력하면 사용 및 설정법 알려줌. 

  - git status : 저장소 상태 체크 - 변경사항 여부 / git 내 파일 목록 / 작업중인 브랜치 등 확인 가능

  - git add : 저장소 내에서 작업 뒤 해당 변경사항을 기억하도록 해주는 명령어. 일종의 "스냅샷". git commit -m "___ function modified"과 같이 씀.

  - git branch : 새로운 브랜치 생성 후 및 커밋 타임라인 초기화. git branch "hello"와 같이 씀. 

  - git checkout : 원하는 저장소로 옮겨가주는 명령어. (현)master 브랜치에서 hello 브랜치로 옮겨가려면 git checkout hello와 같이 쓰면 됨. 

  - git merge : 작업한 브랜치를 다른 협업자들도 볼 수 있도록 master 브랜치에 병합하는 명령어. git merge hello 와 같이 씀. 

  - git push : 로컬 컴퓨터에서 작업 뒤 깃허브에 커밋 사항을 저장하는 명령어

  - git pull : 깃허브로부터 커밋/변경사항 등을 가져오는 명령어

    
